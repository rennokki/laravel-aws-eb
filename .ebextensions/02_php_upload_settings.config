# Set the desired values to control file upload size.
# post_max_size value should be equal to or greater than upload_max_filesize
files:
  "/etc/php.d/95-file-uploads.ini" :
    mode: "000755"
    owner: root
    group: root
    content: |
      upload_max_filesize = 24M
      post_max_size = 32M
